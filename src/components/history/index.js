// import { useReducer } from "react";
// // import apiReducer, { addAction, removeAction, emptyAction } from "../../app";

// function History() {
  

  

//   return (
//     <>
//       <h1>History</h1>

//       <form onSubmit={handleSubmit}>
//         <input apiName="api" />
//         <button>Add API</button>
//       <button onClick={() => dispatch(emptyAction())}>Clear All</button>
//       </form>
//       <ul>
//         {state.api.map((api, indx) => {
//           return (
//             <li key={indx} onClick={() => dispatch(removeAction(api))}>
//               {api}
//             </li>
//           );
//         })}
//       </ul>
//     </>
//   );
// }

// export default History;
